% '
%% 
% #noc 
% #md§§% #endmd
% #plain§§% #render
% 
%-- 
%{§§%}
zeros(m, n)
ones(m, n)
eye(n)
rand(m, n)
randn(m, n)
linspace(a, b, n)
size(A)
length(v)
numel(A)
A(end)
inv(A)
det(A)
norm(v)
dot(a, b)
cross(a, b)
eig(A)
[V, D] = eig(A)
lu(A)
chol(A)
qr(A)
svd(A)
sum(v)
prod(v)
cumsum(v)
diff(v)
max(v)
min(v)
sort(v)
find(v)
interp1(x, y, xq)
for k = 1:n§    §end
while cond§    §end
if cond§    §end
if cond§    §else§    §end
if cond§    §elseif cond§    §else§    §end
switch expr§    case a§        §    otherwise§        §end
try§    §catch err§    §end
function y = f(x)§    §end
disp(x)
fprintf('%.4f\n', x)
sprintf('%.4f', x)
num2str(x)
tic
toc
plot(x, y)
plot(x, y, '-o')
hold on
hold off
xlabel('x')
ylabel('y')
title('t')
legend('a', 'b')
grid on
axis equal
subplot(m, n, p)
scatter(x, y)
bar(y)
contourf(X, Y, Z)
surf(X, Y, Z)
mesh(X, Y, Z)
colorbar
disp('==== TÍTULO ====')
disp('=== Título ===')
disp('== Título ==')
disp('= Título =')
%'&lt;div&gt;§%'&lt;/div&gt;
%'&lt;div class=&quot;fold&quot;&gt;§%'&lt;h4&gt;Heading&lt;/h4&gt;§%'Folded content§%'&lt;/div&gt;
%'&lt;ul&gt;§%'&lt;li&gt;Item 1&lt;/li&gt;§%'&lt;li&gt;Item 2&lt;/li&gt;§%'&lt;li&gt;Item 3&lt;/li&gt;§%'&lt;/ul&gt;
%'&lt;ol&gt;§%'&lt;li&gt;Item 1&lt;/li&gt;§%'&lt;li&gt;Item 2&lt;/li&gt;§%'&lt;li&gt;Item 3&lt;/li&gt;§%'&lt;/ol&gt;
%'&lt;table class=&quot;bordered&quot;&gt;§%'&lt;thead&gt;§%'&lt;tr&gt;&lt;th&gt;col 1&lt;/th&gt;&lt;th&gt;col 2&lt;/th&gt;&lt;/tr&gt;§%'&lt;/thead&gt;§%'&lt;tbody&gt;§%'&lt;tr&gt;&lt;td&gt;11&lt;/td&gt;&lt;td&gt;12&lt;/td&gt;&lt;/tr&gt;§%'&lt;tr&gt;&lt;td&gt;21&lt;/td&gt;&lt;td&gt;22&lt;/td&gt;&lt;/tr&gt;§%'&lt;/tbody&gt;§%'&lt;/table&gt;
x = 5;§fprintf('%s = %.4g\n', 'x', x)
L = 3.50;§fprintf('%s = %.2f %s\n', 'L', L, 'm')
x = 5;§disp(['Resultado: ', num2str(x)])
x = 5;§s = sprintf('%s = %.4g', 'x', x)
x = 3.14159;§fprintf('%.4f\n', x)
valor = 12.3456;§fprintf('%-12s %10.3f\n', 'nombre', valor)
disp('texto')
disp('----------------------------------------')
disp(' ')
x = linspace(0, 10, 100);§y = sin(x);§plot(x, y)§grid on
x = 0:10;§y = x.^2;§plot(x, y, '-o')§grid on
x = linspace(0, 2*pi, 100);§plot(x, sin(x))§hold on§plot(x, cos(x))§hold off§legend('sin', 'cos')
x = rand(50, 1);§y = rand(50, 1);§scatter(x, y)
y = [3 7 2 5 4];§bar(y)
datos = randn(1000, 1);§histogram(datos)
n = 0:20;§stem(n, sin(n/3))
[X, Y] = meshgrid(-3:0.2:3);§Z = X.^2 - Y.^2;§surf(X, Y, Z)§colorbar
[X, Y] = meshgrid(-3:0.2:3);§Z = sin(sqrt(X.^2 + Y.^2));§mesh(X, Y, Z)
[X, Y] = meshgrid(-3:0.2:3);§Z = X.^2 - Y.^2;§contourf(X, Y, Z)§colorbar
t = linspace(0, 10*pi, 200);§plot3(sin(t), cos(t), t)§grid on
[X, Y] = meshgrid(-2:0.5:2);§quiver(X, Y, -Y, X)
xlabel('x')§ylabel('y')§title('Título')
legend('curva 1', 'curva 2')
for k = 1:5§    disp(k)§end
s = 0;§for k = 1:10§    s = s + k;§end§s
for i = 1:3§    for j = 1:3§        disp([i, j])§    end§end
k = 1;§while k &lt;= 5§    disp(k)§    k = k + 1;§end
x = 5;§if x &gt; 0§    disp('positivo')§end
x = -2;§if x &gt; 0§    disp('positivo')§else§    disp('negativo')§end
x = 0;§if x &gt; 0§    disp('positivo')§elseif x &lt; 0§    disp('negativo')§else§    disp('cero')§end
n = 2;§switch n§    case 1§        disp('uno')§    case 2§        disp('dos')§    otherwise§        disp('otro')§end
try§    error('algo falló')§catch err§    disp(err.message)§end
for k = 1:10§    if k &gt; 5§        break§    end§    disp(k)§end
function y = mifun(x)§    y = x^2;§end
function [s, p] = sumprod(a, b)§    s = a + b;§    p = a * b;§end
classdef Punto§    properties§        x§        y§    end§    methods§        function obj = Punto(x, y)§            obj.x = x;§            obj.y = y;§        end§        function d = norma(obj)§            d = sqrt(obj.x^2 + obj.y^2);§        end§    end§end
f = @(x) x.^2;§a = 0; b = 1;§I = integral(f, a, b)
x = linspace(0, 1, 100);§y = x.^2;§area = trapz(x, y)
a = 0; b = 1; n = 100;§h = (b - a)/n;§s = 0;§for k = 1:n§    x0 = a + (k-1)*h;§    x1 = a + k*h;§    s = s + 0.5*h*(x0^2 + x1^2);§end§s
syms x§I_g = gaussint(x^2, x, 0, 1)
s = sum((1:100).^2)
s = 0;§for k = 1:100§    s = s + k^2;§end§s
p = prod(1:5)
x = linspace(0, 2*pi, 100);§y = sin(x);§dydx = gradient(y, x);§plot(x, dydx)
v = [1 4 9 16];§d = diff(v)
f = @(x) x^2 - 2;§x0 = 1;§r = fzero(f, x0)
%' Declarar x como variable simbólica:§syms x
%' Derivada de f(x) = x³ + 2x respecto a x:§syms x§f(x) = x^3 + 2*x§df = diff(f(x), x)
%' Integral indefinida de f(x) = x²:§syms x§f(x) = x^2§F = int(f(x), x)
%' Integral definida de f(x) = x² entre 0 y 1:§syms x§f(x) = x^2§I = int(f(x), x, 0, 1)
%' Límite de f(x) = sin(x)/x cuando x → 0:§syms x§f(x) = sin(x)/x§L = limit(f(x), x, 0)
%' Serie de Taylor de f(x) = eˣ alrededor de x = 0:§syms x§f(x) = exp(x)§T = taylor(f(x), x, 0)
%' Simplificar f(x) = (x²−1)/(x−1):§syms x§f(x) = (x^2 - 1)/(x - 1)§s = simplify(f(x))
%' Expandir f(x) = (x + 1)³:§syms x§f(x) = (x + 1)^3§e = expand(f(x))
%' Factorizar f(x) = x²−1:§syms x§f(x) = x^2 - 1§fa = factor(f(x))
%' Resolver f(x) = 0 con f(x) = x² − 4:§syms x§f(x) = x^2 - 4§sol = solve(f(x) == 0, x)
%' Sustituir x = 3 en f(x) = x²:§syms x§f(x) = x^2§v = subs(f(x), x, 3)
%' Jacobiano de F = [x·y, x + y] respecto a [x, y]:§syms x y§F = [x*y, x + y]§J = jacobian(F, [x, y])
%' Exportar f(x) = x² + 1 a código LaTeX:§syms x§f(x) = x^2 + 1§L = latex(f(x))
%&quot; Titulo de seccion
%&quot;{negro} Titulo en negro
%'-----
%'&lt; texto a la izquierda
%'| texto centrado
%'&gt; texto a la derecha
%'* texto en negrita
%'/ texto en italica
%'_ texto subrayado
%'{center,azul,negrita} texto centrado azul y negrita
%'&lt;table class=&quot;bordered&quot;&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;Col 1&lt;/th&gt;&lt;th&gt;Col 2&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody&gt;&lt;tr&gt;&lt;td&gt;a&lt;/td&gt;&lt;td&gt;b&lt;/td&gt;&lt;/tr&gt;&lt;tr&gt;&lt;td&gt;c&lt;/td&gt;&lt;td&gt;d&lt;/td&gt;&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;
df__dx = diff(f, x)
  %' ms
%'\&gt; muestra el signo &gt; literal
min(x, y)
max(x, y)
fzero(@(x) x, x0)
for k = 1:n&#10;    &#10;end
trapz(x, y)
integral(@(x) x, a, b)